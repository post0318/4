# PRD — 브라질 국채 매수 프로세스 자동화

## 목적

브라질 국채(NTN-F) 매수 주문을 준비하는 과정을 자동화한다. 담당자가 환율과
시세를 일일이 찾고 수량을 손계산하던 것을 없애고, 원화 투자금액 하나만 입력하면
주문 이메일까지 준비되게 한다.

## 핵심 요구사항

### 1. 환율 자동 표시
- 원/달러(USD/KRW), 달러/헤알(USD/BRL), 원/헤알(KRW/BRL)을 자동으로 표시한다.
- 원/헤알은 USD/KRW ÷ USD/BRL 파생값으로 계산해 표시값과 수량계산을 일치시킨다.
- 출처: Frankfurter.dev(ECB 기준). 중간환율이며 화면에 그 사실을 명시한다.
- 새로고침 버튼으로 재조회.

### 2. 종목 자동 표시
- 현재 거래 중인 NTN-F 목록을 자동으로 표시한다(만기일이 지난 종목 제외).
- 종목 선택 시 ISIN, 종목명(한글/포르투갈어), 매수수익률, 만기일, 표면이율을 표시한다.
- 매수수익률 = 스냅샷의 Taxa Venda(테조우로가 투자자에게 파는 금리 = 투자자
  매수 기준).
- ISIN이 교차확인되지 않은 종목(현재 2037)은 "확인 필요" 뱃지를 표시한다.

### 3. 투자금액 입력 → 수량 산출
- 원화투자금액(KRW)을 입력한다.
- 환전된 달러금액 = KRW ÷ (USD/KRW) 을 자동 반영한다.
- 헤알 환산액 = USD 금액 × (USD/BRL).
- 매수단가(PU)는 매수수익률로 ANBIMA 표준 공식(Business/252 복리 할인)을 써서
  결제일(D+0 브라질 영업일) 기준으로 재계산한다. 스냅샷의 PU 값은 쓰지 않는다.
- 매수가능수량 = 내림(헤알 환산액 ÷ PU). **정수, 1좌 = 액면 R$1,000.**
- 실매수금액(BRL/USD/KRW 환산)과 잔여현금을 함께 표시한다.

### 4. 이메일 발송
- 산출된 수량과 주문 명세(종목·ISIN·PU·환율·금액·주문일·결제일)를 이메일 본문으로
  구성해 수신자에게 발송한다.
- 수신자 주소는 `ORDER_EMAIL_TO` 환경변수를 기본값으로 채우고, 화면에서 수정
  가능하다.
- (현 단계) 실제 전송은 stub. 전송 수단(Resend / Gmail SMTP)은 추후 결정해
  `sendEmail()` 어댑터에 연결한다. 본문 생성·수신자·확인 UI·서버 검증은 완성.

### 5. 발송 전 확인
- "환율·종목·수량·수신자를 모두 확인했습니다" 체크박스를 켜야 발송 버튼이
  활성화된다.
- 발송 버튼을 누르면 주문 요약 모달이 한 번 더 뜨고, 거기서 확정해야 실제 요청이
  나간다.
- 서버(`/api/send-order`)는 클라이언트가 보낸 PU·수량을 신뢰하지 않고 다시
  계산해 대조한다. PU가 0.5% 넘게 다르거나 수량이 다르면 발송을 거부한다(422).

## 계산 규칙 요약

| 항목 | 계산 |
|---|---|
| 결제일 | 주문일 기준 D+0, 브라질 영업일(주말 + ANBIMA/B3 국경일 제외)이 아니면 다음 영업일 |
| 실효 반기쿠폰 | 1,000 × ((1.10)^(1/2) − 1) ≈ 48.8089 |
| PU (dirty) | Σ CF ÷ (1 + 매수수익률)^(결제일→현금흐름 브라질영업일수 ÷ 252), 소수 4자리 올림 |
| 달러 환전액 | KRW ÷ (USD/KRW), 소수 2자리 |
| 헤알 환산액 | USD × (USD/BRL), 소수 2자리 |
| 매수가능수량 | ⌊헤알 환산액 ÷ PU⌋ (정수) |

## 범위 밖 (후속)

- 실제 이메일 전송 연동
- FX 스프레드/우대율, 실시간 시세, 주문 이력 저장
- 소수(0.01좌) 매수, 재투자/분할 주문
